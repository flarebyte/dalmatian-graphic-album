/** Illustrations and Panels */

table Entity {
    metadata: Metadata;
}

table Shape {
    metadata: Metadata;
    drawing: [Draw];
    entityId: uint16;
}

table Illustration {
    metadata: Metadata;
    data: anyUint;
}

struct Clipping {
    illustrationId: uint16;
    x: Fraction;
    y: Fraction;
    width: Fraction;
    height: Fraction;
}

union Composition { Clipping }

table Panel {
    metadata: Metadata;
    composition: [Composition];
}